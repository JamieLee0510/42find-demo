# ── 第六组 过程材料：都不入库。日期一律打头方能一路排序；均禁裸文件 ──
# 构建产物（可重建）：只许 YYYYMMDD-HHMMSS/，一次构建一个——按天分会撞车
_build/
# 一次性过程材料（用完即弃）：只许 YYYYMMDD-slug/
_tmp/
# 快照：只许 YYYYMMDD-slug/
_archive/*
# 例外：需入库可追溯的下线件，目录名以 -delisted 结尾，带 README 说明为什么下线
!_archive/*-delisted/

# ── 第二组 外部参考：本地留存，仅 README 入库 ──
resources/**
!resources/README.md

# 注：notes/ 装同事写的、要读的东西，**入库**。只有你自己看的速记不放这里，放 _tmp/

# 大件（本地 + 云盘，绝不入库；按需加例外）
*.key
*.pptx
*.pdf
*.mp4
*.mov
*.zip

# 密钥（永不入库）
.env
.env.local
.env.*.local

# 系统
.DS_Store
__pycache__/
*.pyc
node_modules/
.venv/

# Rust 构建产物（属于第六组「过程材料」：扔的，不入库）
# ⚠️ Cargo.lock **要入库**——这是一个出二进制的项目，锁文件是「装的是不是同一个版本」的另一半。
target/
