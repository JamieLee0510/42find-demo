# CLAUDE.md — 42find · 本地全文检索工具（42find）

> AI 开工必读：本文件 + **`.42cog/` 四份**（`intent` 意向书 · `real` 现实约束 · `cog` 认知模型 · `meta` 项目身份）+ `state/board.md` + `state/memory/MEMORY.md`。
> 本文只留**祈使句与硬约束**：目录速查见 `README.md`，命名与格式规约见 `specs/README.md`，工具与降级见 `scripts/check-tools.sh`。
> **这份文件应该越用越长——但由你和它一起长，不是一开始就发给你一堆。**

## 一、方向

**唯一正本：`.42cog/intent.md` 最上面那一句。** 冲突时以它为准；真难题 · 不做什么 · 真相源权重 · 作品怎么算合格，都在同一份文件里。
别处一律指针，**绝不抄副本**（方向会改，抄出去的那份改不到，就成了错的）。
**分工线**：`.42cog/` 是**陈述句**（这个系统是什么样）· 本文是**祈使句**（你该怎么干）· `specs/` 是**标准**（产出该长什么样，含命名）。三者不许互相串门。

## 二、开工协议

**先分流，再冒烟，必要时补一次有界小实验。** 确定的你执行；需要我拍板的，充分调研后列出来等我定。动手前先冒烟确认上下文够、权限够。
**任务编排三法**：合并同类项（先分组，同类同批）· 确定性分流（见上）· 开工前先同步（别基于陈旧的本地状态动手）。
**意外矩阵**：提前写好「如果 X，那么 Y」，落点只有一个——**永不停摆**。单条卡住跳下一条；**同一类错误犯到第三次，必须沉淀成一条规则**，写进本文件或 `state/memory/`。
**护栏**：破坏性命令（批量删除、强制覆盖、强推）先拦下来。**拦得住，但不堵死**——确需执行的加显式前缀重发即放行，并留痕。「文件还在」和「工作还在」，是两回事。
**并行**：用 Git 的 worktree（工作副本，换工具带得走）。一个分身占一份完整硬盘空间，**先看盘还剩多少再决定开几条**；**别套娃**（worktree 里再开 worktree，路径越缠越乱）。⚠️ **退出时它会问你「有未提交的改动，留不留」——那一问别手快点掉。**
**自检不是再读一遍**（再读一遍只会再确认自己是对的），要**几个互相对抗的视角同时审**，视角越分化越管用。现成的在 `.claude/workflows/`；换谱系评审见 `scripts/check-tools.sh` 头部。

## 三、三层验证：能用规则判的，绝不留给人判

| 层 | 谁判 | 落在哪 |
|---|---|---|
| 规则验证 · 判对错 | 机器，瞬时近免费 | `scripts/` 下的脚本。**改完 `plugin.json` / `42plugin.json` 必跑 `python3 scripts/check-manifests.py`**——名字多一个连字符、首字符用了数字，肉眼扫不出来，装的时候才报错 |
| 判据验证 · 判好坏 | AI 按写下的判据代判 | 技能内置判据 · `specs/` 的正例与负例 |
| 实效验证 · 判成败 | 人与世界一起判 | 定标与验收——四步里唯一不能交给 AI 的一步 |

判不了的那一截就是**验证红线**，留给人。

## 四、铁律

1. **可以写错，不可以空着**：收敛方向必须有一句。空着的方向会被平均值填满。
2. **`hi.md` 神圣**：人类自由手稿，AI 只读，绝不修改、重排、格式化、加提示语。
3. **别人的材料**：`vault/raw/` 只读；`notes/` 不改不代提交；`resources/` 读懂后自己写，**绝不 copy-paste**。动了 `notes/` 是冒犯，抄了 `resources/` 是侵权。
4. **人手为界**：经人手的产物 = 原件，不可弃；未经人手的试错才可丢。
5. **旧实践迁就新理念**：旧产物是**事实**的真相源，不是**标准**的真相源。冲突时改旧产物。
6. **输出超过三百 token 就写文件**（约两三百汉字），对话只留路径 + 一句摘要。按寿命分流：一次性→`_tmp/`；状态→`state/board.md`；经验→`state/memory/`；需追溯→`docs/NNN_`。
7. **唯一编码**：同样的内容只留一份，不留副本；引用只给路径与行号，不贴全文。版本走提交链，不留 `v2`/`-final`；改动前先提交；**绝不用 `cp` 覆盖被改过的文件**。命名见 `specs/README.md`。
8. **大文件不进版本库**：单文件超 10 MB 先想清楚，超 50 MB 一律不进。走 `git-lfs` 或放 NAS/云盘，仓里只留**路径 · 哈希 · 一句说明**（`shasum -a 256 <文件>`）。⚠️ 仓外**没有远端副本**，删之前先确认另一处还在。**厚材料同理**（几十上百页的资料，即使不到 10 MB）：先整理一份索引，平时只带索引，要细节再回原件。
9. **每轮必更 `state/board.md`**；里程碑蒸馏进 `state/changelog.md`（变了什么 / 为什么变 / 对你意味着什么）。
10. **新顶层目录 = 协议事件**：先答两问（作品怎么验证？真相源是谁？），记 changelog，人拍板。
11. **反意图污染**：整理人的口头指令时别扩写稀释——十个强信号扩成千字会稀释三倍。保护最强意图。
12. **提示词与外发**：提示词**绝不直接写进命令行**，一律先落成文件再让命令读它（拼进 shell 有被执行的风险，且各家转义处理不一致）。发出去之前过一道：**这份材料有没有权限外发？密钥、个人信息、未公开内容摘掉了吗？**
13. **安全**：密钥进 `.env.local` 永不入库；含个人隐私的数据不入库；删除、对外发布、批量改名等不可逆动作前先确认。
14. **放权边界**：**每步先说要干什么，做完简要汇报。**
    - **不用逐条等确认**：`git commit`（改动自己提交）。
    - **仍要先问**：`git push`、装工具、建/删/改文件之外的一切不可逆动作。
    - 其余照三、四条与本条上一句走。

## 五、本项目特有的硬约束

<!-- 这一节现在是空的，这是对的。
     跨项目通用的规矩上面已经写完了；这里只放**这个系统**独有的那些。
     不要预先设想，等踩到第一次坑再补一条——名字就叫那次坑的名字。 -->

（还没有。遇到第一次教训就补。）

## 六、约定

沟通与文档用中文，文件名用英文 ｜ 单次回复尽量短，长内容写文件后给路径 + 一句摘要 ｜ 提交信息格式见 `specs/README.md`

---

*由 `aias-meta-init` 生成于 2026-09-05。*
